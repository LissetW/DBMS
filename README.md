# DBMS
This repository contains the implementation of a Database Management System (DBMS) developed in the C programming language and designed to run on Linux environments. The system follows a client–server architecture, using TCP/IP sockets to enable remote communication between clients and the database server.
